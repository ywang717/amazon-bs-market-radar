# V2.2.6 pipeline checkpoint — 2026-09-10

## Verified state

- Market date remains Pacific: 2026-09-09.
- Public overview API independently returned 2026-09-09 and 30 raw observations for each of pressure_washers, sump_pumps, pressure_washer_accessories.
- Local snapshot has 30 unique ASINs and ranks 1–30 in each market.
- Snapshot SHA256 before and after recovery: `70f749094044c419d5d8607f11fc980075460262bb66c7f1d0bf0e17290cdcac`.
- Operational health independently returned HEALTHY, valid receipt, snapshot 90 / PostgreSQL 90, three PDFs, verified backup.
- Existing publication record: 2026-09-10T01:51:11Z, PUBLISHED, 90 observations, three reports uploaded.
- New production recovery test: 2026-09-10 10:26:25–10:26:32 local. Health succeeded, existing dashboard recovery action returned SKIPPED_ALREADY_PUBLISHED for 2026-09-09. No recrawl, date rewriting, or historical snapshot modification.

## Root-cause evidence

1. Local 08:00 and 08:30 collection attempts failed with CATEGORY_NOT_COMPLETE / INVALID_RANKS. Detailed canonical diagnostics were overwritten by the later successful attempt; precise browser/network cause is not proven.
2. Local 09:00 and 09:15 publication failed because the day's snapshot did not yet exist.
3. Manual collection at 09:43–09:50 succeeded; independent manual publication at 09:50–09:51 succeeded.
4. The daily wrapper previously stopped after local processing/health; publication depended on separate wall-clock tasks. Late recovery therefore had no automatic publication handoff.

## Implemented this task (preserving pre-existing dirty changes)

- Daily wrapper now calls the existing registered dashboard recovery action after successful production processing, including healthy recovery.
- Reuses registered runtime, DPAPI-protected secret and proxy configuration; no second crawler or publisher.
- Passes the processed Pacific MarketDate through the protected publisher rather than recomputing it at publication time.
- Publisher nonzero exit propagates into daily failure; publication status and start time are recorded.
- Writes uniquely named per-run state and retains newly produced collection diagnostics so later attempts do not overwrite failure evidence.
- No web source, schema, historical data, crawler or timezone changes made by this task.

## Tests

`ScheduledDailyPublication.Tests.ps1`, `ScheduledDailyAuto.Tests.ps1`, `DashboardPublishing.Tests.ps1`: 45 passed, 0 failed.

New tests observed failing before fixes cover date propagation, publisher failure, unconfigured publisher, successful primary/healthy recovery handoff, failed collection suppression and per-run diagnostic persistence.

`git diff --check`: clean. This checkpoint does not attribute pre-existing dirty edits to this task.

## Scheduler

Existing tasks enabled. Next local runs: 2026-09-11 08:00 primary, 08:30 recovery, 09:15 dashboard recovery. These correspond to the existing Pacific date calculation; schedule clock was not changed.

Tasks use Interactive logon, so this remains a local-machine pipeline requiring the user session and machine availability, not an always-on cloud scheduler. Secrets remain DPAPI-protected for the existing user.

## Remaining data-integrity boundary

- `web/app/api/sync/v1/bundles/route.ts` deletes all observations/category days for a market date before inserting the supplied categories. A partial single-market payload could therefore remove other markets for that date.
- `snapshots` has one receipt per market date. Both analysis-reports and seller-intelligence report synchronization bind their report cohort to this receipt.
- Merely changing DELETE predicates to category-scoped predicates would preserve rows but would not establish truthful per-market receipt provenance for mixed captures. Do not treat that local SQL change alone as complete independent-market persistence.
- Collection also rejects the whole attempt when one category fails. Independent successful-category persistence requires coordinated capture/receipt/publish/report-contract handling, not a UI-only change.
- No partial production payload was sent and no safety checks were relaxed.

## Acceptance status

AUTO DAILY UPDATE: NOT PASSED. The production recovery path works, but the required new valid date greater than the pre-test website date was not produced; pre/post are both 2026-09-09. Original scheduled collection failure cause and independent-market persistence remain open.

P1 Product Polish, browser/screenshot QA and production build for V2.2.6 are not started. Keep the PRD's P0 gate. V2.2.5 remains the published website version.

## Continuation checkpoint — 2026-09-11 01:22 local

The sections above record the initial checkpoint, not the current implementation inventory. Following user approval, local changes now include category-scoped atomic bundle persistence, an additive category receipt ledger/guard migration, receipt-bound scoped report support, and partial capture preservation. These changes have NOT been deployed.

- Collector retains individually complete categories in unique partial artifacts without replacing the canonical full snapshot; the overall failed attempt stays failed.
- Scoped receipt/bundle authorization accepts a verified complete category from a partial capture, while rejecting authorization for the incomplete category or the full day.
- Updated three test references to read canonical source URLs from the existing registry-generated configuration, not a removed collector constant.
- Python full regression: 106 passed. DashboardSyncBundle Pester: 10 passed, 0 failed.
- Earlier local web contract/route/migration tests: 63 passed; local production build passed. Neither result constitutes production deployment or end-to-end acceptance.
- Scheduler inspection at 2026-09-11 01:22 +08:00: next primary remains 08:00, recovery 08:30, publisher 09:00/09:15. Last scheduled daily/publish results remain failures from September 10; the separate successful recovery is documented above.
- No MarketDate conversion, historical snapshot rewrite, production partial payload, or scheduler change in this continuation.

Still required: connect partial artifacts through existing orchestration/PG/publisher/report paths; close legacy unbound report writes on receipt-ledger-backed dates; default-test registration and final regression; deploy verified server support before any partial production publication; obtain genuine new-valid-date end-to-end evidence. P0 remains NOT PASSED; P1 remains gated.

### Subsequent continuation

- Closed legacy unbound daily report insert paths (single report and cohort) on category-receipt-backed dates using SQL write-time conditions. Regression injects the ledger immediately before mutation; neither path writes a report. Old dates without ledger provenance retain legacy compatibility.
- Collector stdout now carries `PartialSnapshotPath` and `CompleteCategories`, while retaining failure exit status and leaving canonical snapshots untouched. The existing launcher forwards this control message unchanged; orchestration still needs to consume it.
- Fresh verification: Python 106/106, web receipt/contract/route/migration tests 64/64, production build PASS. Not deployed; P0 end-to-end gate remains unverified.
