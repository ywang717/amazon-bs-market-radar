# V2.4 第一轮 Correctness Audit 基线

日期：2026-09-07
范围：V2.4 第一轮 Stage 1；本记录只固化现状、证据、复现命令和回归基线，不实施修复。

## 审计边界与基线

- 工作树：`C:\Users\ASUS\Documents\亚马逊bestseller榜单监控\.worktrees\v2-4-round1`
- 审计起始 HEAD：`03ad6f114cd70496a74278535d1b49beaaa27108`
- 起始 `git status --short`：无输出（干净工作树）。
- 起始 `git diff --check`：无输出。
- 本提交唯一新增业务相关文件为本审计文档；未修改历史 Snapshot、生产配置、生产代码或测试。

## Findings

| ID | Area | Evidence | Status | Target |
| --- | --- | --- | --- | --- |
| C-01 | Product Identity | 基线：详情页未传 brand/product type。现在 `web/tests/rendered-html.test.mjs` 的跨路由合同以同一 `B000000001` metadata fixture 同时渲染 Products 列表与 Product Detail；两处都显示 canonical `Westinghouse` 与“电动”，且都拒绝故意冲突的 `raw_brand=Raw Marketplace Brand`。 | RESOLVED | 同一 ASIN 的详情与列表 canonical identity 一致 |
| C-02 | Deal validation | 基线：`discount <= currentPrice` 错误拒绝合法大额优惠。现在 `web/tests/ui-intelligence.test.mjs` 的 `keeps a verified Amazon saving even when it exceeds current price` 及 `web/tests/rendered-html.test.mjs` 的 `renders verified discounts from the live dashboard on ranking and product pages` 验证共享语义。 | RESOLVED | 合法大额优惠保留 |
| C-03 | Review event | 基线：Signal 使用 compact number，详情无 Review Event。现在 `web/tests/ui-intelligence.test.mjs` 的 `formats review events exactly instead of compacting evidence` 及 `web/tests/rendered-html.test.mjs` 的 `renders exact review change events on product detail` 验证精确前后值和增量。 | RESOLVED | 精确事件格式与时间线 |
| C-04 | Time window | 基线：Overview 未显式标注 `1D`。现在 `web/tests/rendered-html.test.mjs` 的 `renders the signal-first market dashboard` 和 `renders dense market intelligence routes with shared product and rank semantics` 验证 `1D`/`7D`/`30D`；`web/tests/live-dashboard-data.test.mjs` 验证上一有效市场日。 | RESOLVED | 指标窗口明确 |
| C-05 | Product specs | 基线：规格只按 Category 过滤。现在 `web/tests/product-specs.test.mjs` 的 `uses pressure semantics only for pressure-washer machines` 验证 Category + Product Type 的 PSI 边界；`web/tests/rendered-html.test.mjs` 验证详情渲染。 | RESOLVED | Category + Product Type |
| C-06 | Brand wording | 基线：部分页面使用“市场席位”。现在 `web/tests/rendered-html.test.mjs` 与 `web/tests/ui-accessibility.test.mjs` 都要求“榜单席位”并拒绝旧词“市场席位”。 | RESOLVED | 统一“榜单席位” |
| R-01 | Category source | 生产定义至少分散于 `config/best-sellers-sources.json:5-28`、`web/lib/catalog.ts:1-5`、`config/categories.json` 和 `config/v2-product-classification.json`；尚无单一 Registry 作为这些调用方的权威来源。 | OPEN | Registry 为唯一权威来源 |

## Immutable Compatibility Facts

以下事实来自当前生产配置与 V2.4 设计规范，后续 Registry/正确性工作必须保持兼容：

| Category Key | 中文名称 | Amazon Node | Source URL | 当前 Web segments |
| --- | --- | --- | --- | --- |
| `pressure_washers` | 高压清洗机 | `552856` | `https://www.amazon.com/Best-Sellers-Patio-Lawn-Garden-Pressure-Washers/zgbs/lawn-garden/552856` | `all`, `machines`, `electric`, `gas`, `cordless` |
| `sump_pumps` | 污水泵 | `680335011` | `https://www.amazon.com/Best-Sellers-Tools-Home-Improvement-Sump-Pumps/zgbs/hi/680335011` | `all` |
| `pressure_washer_accessories` | 高压清洗机配件 | `3023451` | `https://www.amazon.com/Best-Sellers-Patio-Lawn-Garden-Pressure-Washer-Parts-Accessories/zgbs/lawn-garden/3023451` | `all`, `surface_cleaners`, `guns`, `hoses`, `nozzles`, `other` |

- Marketplace 为 `AMAZON_US`，目标榜单容量为 30（`config/best-sellers-sources.json:2-3`）。
- 高压清洗机原始榜单中的配件与独立 `pressure_washer_accessories` 榜单必须继续严格区分，不能互相冒充数据源。
- `sump_pumps` 的名称、Key 和 Node `680335011` 保持不变；数据库继续以字符串 `category_key` 兼容历史数据，不在本审计引入迁移。
- 历史 Raw Snapshot 只读；本审计不重写、不回填、不重新解释。

## Reproduction commands and raw baseline

在本工作树根目录执行：

```powershell
git status --short
git rev-parse HEAD
git diff --check
```

审计起始结果：HEAD 为 `03ad6f114cd70496a74278535d1b49beaaa27108`；status 与 diff-check 均无输出。

PowerShell/Pester：

```powershell
pwsh -NoLogo -NoProfile -Command '$r = Invoke-Pester -Path tests/BestSellersDataSemantics.Tests.ps1 -PassThru; if ($r.FailedCount -gt 0) { exit 1 }'
```

原始结果：`Passed: 48 Failed: 4 Skipped: 0 Pending: 0 Inconclusive: 0`，命令退出码 `1`；执行环境中唯一可用的 Pester 为 `3.4.0`。4 个失败均来自 `tests/BestSellersDataSemantics.Tests.ps1` 中对 unsupported/non-canonical classification config 的 `Should Throw` 断言（报告位置分别为 line 115 和 line 143 的断言）。这些旧断言的通过/失败不能视为 C-01 至 C-05 已解决；它们反而是后续修复需重新审视的回归基线。

Web Node：

```powershell
Push-Location web
node --test tests/ui-intelligence.test.mjs tests/market-context.test.mjs tests/valid-market-days.test.mjs
Pop-Location
```

原始结果：`tests 55`、`pass 55`、`fail 0`、`cancelled 0`、`skipped 0`，命令退出码 `0`。

## Task 1 baseline status

`BASELINE_RECORDED_WITH_CONCERNS`

Task 1 完成时只记录基线，不代表正确性问题已修复。当时 C-01 至 C-06 与 R-01 均保持 `OPEN`；PowerShell 基线存在 4 项已知失败，后续任务必须显式处理并更新相应验收证据。除本审计文档外，本 Task 不产生代码、配置、测试或数据变更。

## Commit

- 审计基线提交：`c34b66d4cefc8197b742ae2c500868cea774a769` (`docs: audit v2.4 correctness baseline`)

## Fix-round 1 review update (2026-09-07)

- 按 review 要求补充 C-02 的精确错误谓词与实现证据：当前 `validateDealDisplay` 只有在 `discount <= currentPrice` 时才判定有效；当优惠金额大于当前售价时，该条件拒绝该 deal，即使 Amazon 证据可能合法。目标行为是不能仅因“优惠金额 > 当前售价”丢弃优惠；应改由缺失、类型错误、负值、不可信来源或明确证据冲突决定待确认状态。
- 补充基线运行时信息：PowerShell 测试运行于唯一安装的 Pester `3.4.0`。
- 本轮仍只修改本审计文档；未修改生产代码、测试、配置或历史 Snapshot。
- 只读复核：`rg` 确认 `web/lib/ui-intelligence.ts:466-480` 的谓词与 `PesterVersion=3.4.0`；`git diff --check` 应无输出。

## Task 9 跨层验收更新（2026-09-08）

### 可执行证据

| ID | 测试证据 | 复现命令 |
| --- | --- | --- |
| C-01 | `web/tests/rendered-html.test.mjs` 的 `keeps the machines universe aligned across overview products brands and signals while Raw All preserves accessories`：同一 `B000000001` 在 Products 列表和 Product Detail 均显示 canonical `Westinghouse`/“电动”，并均排除冲突的 raw brand | `Push-Location web; node --test tests/rendered-html.test.mjs; Pop-Location` |
| C-02 | `web/tests/ui-intelligence.test.mjs` 的 `keeps a verified Amazon saving even when it exceeds current price`；`web/tests/rendered-html.test.mjs` 的 `renders verified discounts from the live dashboard on ranking and product pages` | `Push-Location web; node --test tests/ui-intelligence.test.mjs tests/rendered-html.test.mjs; Pop-Location` |
| C-03 | `web/tests/ui-intelligence.test.mjs` 的 `formats review events exactly instead of compacting evidence`；`web/tests/rendered-html.test.mjs` 的 `renders exact review change events on product detail` | `Push-Location web; node --test tests/ui-intelligence.test.mjs tests/rendered-html.test.mjs; Pop-Location` |
| C-04 | `web/tests/rendered-html.test.mjs` 的 `renders the signal-first market dashboard` 与 `renders dense market intelligence routes with shared product and rank semantics`；`web/tests/live-dashboard-data.test.mjs` 的 valid-day 测试 | `Push-Location web; node --test tests/live-dashboard-data.test.mjs tests/rendered-html.test.mjs; Pop-Location` |
| C-05 | `web/tests/product-specs.test.mjs` 的 `uses pressure semantics only for pressure-washer machines`；`web/tests/rendered-html.test.mjs` 的 Product Detail 规格测试 | `Push-Location web; node --test tests/product-specs.test.mjs tests/rendered-html.test.mjs; Pop-Location` |
| C-06 | `web/tests/rendered-html.test.mjs` 的 `renders dense market intelligence routes with shared product and rank semantics`；`web/tests/ui-accessibility.test.mjs` 的品牌席位文案合同均要求“榜单席位”且拒绝“市场席位” | `Push-Location web; node --test tests/rendered-html.test.mjs tests/ui-accessibility.test.mjs; Pop-Location` |
| R-01 | 部分证据：`web/tests/category-registry.test.mjs` 和 `web/tests/market-context.test.mjs` 证明 Web Registry 生成、resolver、Test Category 和 cache 维度；`tests/CategoryRegistry.Tests.ps1` 证明 PowerShell consumers、TargetCount 和 fixture 隔离。两处生产硬编码残留使整体状态仍为 `OPEN`。 | `Push-Location web; node --test tests/category-registry.test.mjs tests/market-context.test.mjs; Pop-Location`；`pwsh -NoLogo -NoProfile -Command '$r = Invoke-Pester -Path tests/CategoryRegistry.Tests.ps1 -PassThru; if ($r.FailedCount -gt 0) { exit 1 }'` |

新增的跨层合同测试进一步证明：

- `web/tests/ui-intelligence.test.mjs` 以指定 machine/accessory fixture 对共享消费者直接取证：Overview observations、Product rows、Market Signals 的 ASIN 集合都精确等于 `[B000000001]`，Brands 的 verified-seat 分母和品牌行也都为同一个 1-ASIN universe；`Raw All` 则精确保留 `B000000001` 与 `B000000002`。
- `web/tests/rendered-html.test.mjs` 以完整 30 席 fixture 补充渲染集成证据：同一 Machines Context 下 Overview、Products、Brands、Market Signals 都有正向 machine-derived 证据（产品标题、canonical 品牌或共同 24 席 count），同时不泄漏 Surface Cleaner/Accessory Co；Rankings `all` 明确渲染两条 probe ASIN 和配件标题。相同 `B000000001` fixture 还证明 Products 与 Product Detail 都使用 canonical `Westinghouse`/“电动”，而不是故意冲突的 raw brand。
- `web/tests/ui-accessibility.test.mjs` 将品牌卡合同锁定为“榜单席位”，并增加旧词“市场席位”的负向断言。
- `web/tests/market-context.test.mjs` 验证 13 个生产/Test Category Segment 在两个 Date 与两个 Window 下生成 52 个唯一 cache keys。
- `web/tests/live-dashboard-data.test.mjs` 在最新有效日之后追加 persisted-incomplete 日与缺失 Category Day 的 failed 日，验证 Exit、Turnover 与 Brand Contraction 仍只从最新有效日对派生。
- `tests/CategoryRegistry.Tests.ps1` 验证 Test Category report keys 唯一，注入 fixture 前后生产 Registry SHA-256 不变，且生产 Registry 不出现 `test_category`。

### 历史 Snapshot 安全

执行：

```powershell
git status --short -- data snapshots
git diff --name-only -- data snapshots
```

两条命令均无输出；Task 9 未生成、重写或回填历史 `data`/`snapshots`。

### Remaining Issues

- `R-01` 保持 `OPEN`：`scripts/python/collect_best_sellers.py:78-82` 仍以 `TRUSTED_SOURCE_URLS` 硬编码三个生产 Category 及 URL。风险是 Registry 新增或修改 Category 后，爬虫的 trusted-source 边界会独立漂移或拒绝合法配置。建议在 Task 10 前将该信任集从 canonical Registry/生成的 collector source config 派生，并增加 stale/fail-closed 测试。
- `R-01` 保持 `OPEN`：`web/lib/sync-contract.ts:5` 仍以 `allowedCategories` 硬编码三个 Category。风险是 Registry 已启用的第四 Category 会在 sync bundle 边界被拒绝，形成 Web/PowerShell 通过但发布失败的分层不一致。建议改为消费生成的 Web Registry Category/Product Type 集合，并用注入 Registry fixture 验证第四 Category。

## Task 10 最终验证更新（2026-09-08）

R-01 两处边界已迁移并验证：

- `scripts/python/collect_best_sellers.py` 从 canonical `config/category-registry.json` 读取 enabled Category 的 trusted source URL、Node 结构和 fail-closed identity；新增 `tests/python/test_collector_registry.py`，注入 Test Category、disabled/unknown/stale URL 均通过安全边界。
- `web/lib/sync-contract.ts` 从 Runtime Category Registry 计算 enabled Category 集合；新增 `web/tests/sync-contract.test.mjs` 注入 Test Category 并验证 unknown/disabled rejection。

R-01 状态：`RESOLVED`。Python collector targeted tests 为 82（含 4 个 Playwright 环境 skip），Web sync contract 14/14 通过。

### 全量验证结果

- PowerShell `Invoke-Pester -Path tests`: 429 passed, 2 failed。MailDelivery sandbox 已补齐 Registry/config 依赖；剩余失败均为环境限制：Pester 运行时不支持 `Add-Type -OutputType ConsoleApplication`，以及 worktree 缺 `.local\python\python.exe`。Registry 相关 224/224、Brand Enrichment 16/16、Category/Analysis 聚焦 128/128 通过。
- Web `pnpm test`: PASS；首轮 Node 162/162，生产 `vinext build` PASS，构建后路由 118/118。
- Web `pnpm lint`: PASS。
- Web `pnpm exec tsc --noEmit`: PASS。补充最小 Cloudflare Worker/D1/R2/Fetcher ambient contract，并修复 sourceUrl、discount narrowing 与 seed fixture 的既有类型债务；未引入 Wrangler 生成的全局运行时迁移。
- `git diff --check`: PASS。

### 最终状态

`CORRECTNESS_RESOLVED_REGISTRY_AUTHORITY_VERIFIED_WITH_ENVIRONMENT_LIMITS`

C-01 至 C-06 与 R-01 均已解决并有可执行证据。Round 1 的生产构建、Web 回归、独立 TypeScript 检查和 Registry/PowerShell 关键测试通过；完整 PowerShell 仍受上述环境/基线限制，已明确记录，不伪报为 PASS。

Remaining Issues：

1. 全量 Pester 的 2 个失败需要在支持 ConsoleApplication 编译且具备完整 `.local` runtime 的环境中复核；当前不影响本轮 Registry 关键套件。
2. Python collector 的 4 个 Playwright 测试因环境缺浏览器运行时而 skip。
